android {
  packagingOptions {
    jniLibs {
      useLegacyPackaging true
      excludes += 'foo'
      pickFirsts += ['bar', 'baz']
      keepDebugSymbols += 'a'
      keepDebugSymbols += 'b'
      keepDebugSymbols += 'c'
    }
  }
}
