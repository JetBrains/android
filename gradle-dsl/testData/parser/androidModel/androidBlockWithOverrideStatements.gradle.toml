# TOML does not really supor this but we have this test to be sure
# we, at least, don't fail in this situration
android.buildToolsVersion = "23.0.0"
android.compileSdkVersion = 23
android.defaultPublishConfig = "debug"
android.dynamicFeatures = [":f1", ":f2"]
android.flavorDimensions = ["abi", "version"]
android.generatePureSplits = true
android.publishNonDefault= false
android.resourcePrefix = "abcd"

[android]
buildToolsVersion = "21.0.0"
compileSdkVersion ="android-21"
defaultPublishConfig = "release"
dynamicFeatures = [":g1", ":g2"]
flavorDimensions = ["abi1", "version1"]
generatePureSplits = false
publishNonDefault = true
resourcePrefix = "efgh"
