#Gradle
org.gradle.jvmargs=-Xmx2048M -Dkotlin.daemon.jvm.options\="-Xmx2048M"
org.gradle.parallel=true
# kotlin compiler tries to execute in daemon mode (fastest) and fails inside a bazel sandbox
# due to inaccessible workDir, then fallbacks to in-process (slower) mode.
# Defining an accessible workDir has shown ~5s of improvement on average
# https://kotlinlang.org/docs/gradle-compilation-and-caches.html#defining-kotlin-compiler-execution-strategy
systemProp.kotlin.daemon.custom.run.files.path.for.tests=build/kotlin-daemon-runfiles

#Kotlin
kotlin.code.style=official

#Android
android.useAndroidX=true

#MPP
kotlin.mpp.enableCInteropCommonization=true
kotlin.mpp.androidSourceSetLayoutVersion=2
