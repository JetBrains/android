<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="foo.bar2"
          android:versionCode="1"
          android:versionName="1.0">

  <uses-sdk android:minSdkVersion="14"/>

  <application
      android:icon="@drawable/ic_launcher"
      android:label="@string/app_name"
      android:permission="android.permission.READ_CONTACTS">
    <activity
        android:label="@string/app_name"
        android:name="com.sample.service.serviceClass">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"
        <error descr="Protecting an unsupported element with a permission is a no-op and potentially dangerous.">android:<caret>permission="android.permission.SET_WALLPAPER"</error>/>
      </intent-filter>
    </activity>
  </application>

</manifest>