package util;

import app.*;

public class UtilClass {
    public static void doUtilMethod() {
        SomeActivity activity = new SomeActivity() {};
        AppThread thread = new AppThread() {};
    }
}
