apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'

android {
  dataBinding {
    enabled <warning descr="If you plan to use data binding in a Kotlin project, you should apply the kotlin-kapt plugin.">true</warning>
  }
}