apply plugin: 'com.android.feature'

android {
    compileSdkVersion 24
    buildToolsVersion "25.0.0"

    baseSplit true

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 24
    }
}

dependencies {
    featureSplit project(':feature1')
    featureSplit project(':feature2')

    compile("com.android.support:appcompat-v7:+")
}
