apply plugin: 'com.android.library'

android {
    namespace "test.helloworldapp.lib"
    compileSdkVersion 18

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
        }
    }
}
