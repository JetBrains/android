buildscript {
    repositories {

    }
    dependencies {
        classpath "com.android.tools.build:gradle:3.2.0-dev"
    }
}
allprojects {
    repositories {

    }
}
task clean(type: Delete) {
    delete rootProject.buildDir
}