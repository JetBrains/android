apply plugin: 'com.android.atom'

android {
    compileSdkVersion 24
    buildToolsVersion "25.0.0"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 24
    }
}

dependencies {
    compile project(":baseatom")
    compile project(":feature1lib")
}
