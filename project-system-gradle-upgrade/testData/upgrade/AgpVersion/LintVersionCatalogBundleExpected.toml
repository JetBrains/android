[versions]
agpVersion = "8.0.0"
lint = "31.0.0" # agpVersion + 23.0.0

[libraries]
lint-api = { group = "com.android.tools.lint", name = "lint-api", version.ref = "lint" }

[bundles]
lint-api = [ "kotlin-stdlib", "lint-api" ]

[plugins]
agp = { id = "com.android.application", version.ref = "agpVersion" }
